• Fixed the rate failing to load — the app now retries automatically and falls back to a second source if the first is unreachable, so an outage on one website no longer leaves the screen empty
• Gold and silver rates are cross-checked against a backup source, verified to match to the rupee
